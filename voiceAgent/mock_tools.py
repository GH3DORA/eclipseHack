# mock_tools.py — not used in the personal health assistant pipeline
# kept for potential future extension (e.g. connecting to health APIs)
# the main pipeline now relies entirely on the fine-tuned SLM for responses